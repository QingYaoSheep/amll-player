# 剩余视觉能力实施台账

## 2026-09-26 歌词性能优化状态

- **已接线待自动验证：**单次显示链提交、按文档版本与排版键失效、测高排版复用、后台相邻行栅格预热、行图层属性去重、停播静帧跳过、活动可见行 HDR 资格判断、Metal 顶点缓冲复用与 64／128 MiB 图像预算。保留原混色层级、独立音译时间轴和浏览恢复规则。
- **本地通过：**Node 回放与性能报告脚本共 14 项；`git diff --check`。新增 Apple 平台 Core Text 后台栅格一致性、内存警告、HDR 切换不重排及顶点缓冲复用测试，Windows 无法执行。
- **待设备测量：**同输入优化前后 CPU P50／P95／P99、实际 60／120 Hz 帧间隔、GPU 与 drawable 等待、15 分钟丢帧及缓存上限。当前没有可引用的优化前设备轨迹，不能填报提升百分比。
- **待 CI：**Xcode 27 两路任务及 IPA；远端 Git 读取的默认 schannel 后端报 `SEC_E_NO_CREDENTIALS`，OpenSSL 可读取，但推送仍缺少 GitHub 用户凭据。

## 2026-09-26 动态封面修复状态

- **已接线待自动验证：**视频装载前设置 ambient 音频策略；沉浸模式竖版优先、方形回退；首帧后隐藏普通大封面；iPhone 歌词模式只显示静态顶部封面。真实尺寸用于完整适配，底部渐变模糊和可选倒影共用播放器取帧。
- **待设备验收：**同设备 Spotify 播放不中断；竖版视频完整显示与动态背景过渡；歌词显隐、横竖屏、首帧缓冲和失败回退的视觉结果。Windows 无法执行 Apple 真机检查，不能以构建成功替代。
- **待 CI：**Xcode 27 单元/UI 测试，以及直接与测试后两路 IPA；保留之前未提交的回放工程修改。

## 2026-09-25 独立罗马音 TTML 轨道

- 自动生成结果现写为单独的 `.ttml` 缓存文件，并用现有 TTML 解析器读回独立的 `RomanizationTTMLTrack`。原歌词行、逐词时间和来源音译均不被生成器改写。韩语音节只有能一一对应真实来源节点时才得到音节时间；其他可映射词使用真实词节点时间；逐行来源保持逐行时间。
- 原生歌词画布为 TTML 音译创建独立的 Core Text 行和字形遮罩层。它与正文共用播放位置，却按自己 TTML 的时间计算填充；正文 HDR 只作用于正文。生成轨道存在的行在显示副本中隐藏来源音译，生成失败的行继续使用来源回退。原始歌词缓存不变。
- 原音译开关继续控制该轨道；高级设置可以导出当前生成的 TTML。新增 XML 转义、无虚构词时间、来源不变和双行真实画布回归。
- 当前状态：**已接线待自动验证**。Xcode 27 模拟器、两路 IPA、Apple 字体下的对齐和设备视觉验收尚待本次提交的 CI 与设备结果；不把独立数据结构等同于视觉签收。
- 随后按用户反馈补回严格逐词对齐：TTML 仍独立提供音译时间，排版则复用原词容器的 Core Text 位置与换行；生成词有可靠来源归属时使用独立透明音译层覆盖这些位置，无法匹配容器时才使用单独行级排版。正文层不再重复绘制音译。此修正尚待新的 Xcode 27 自动测试与 Apple 设备视觉核对。

## 2026-09-25 Mineradio 离线日韩音译移植状态

固定 IPADIC 的 247,666 条不同 reading/pronunciation 已用开发脚本与 WanaKana 5.3.1 全量比较，零差异；补齐全角标点、促音遇到非假名的边界。命令：`node Scripts/check-mineradio-kana-parity.cjs E:\Antigravity\Mineradio-Spotify`。此检查验证转写映射，不代替 Swift 分词、画布和设备验收。

| 范围 | 当前状态 | 待验证或缺项 |
|---|---|---|
| 来源及资源 | 已固定 Mineradio 工作区引擎、kuromoji 0.1.2 IPADIC、WanaKana 5.3.1；12 个字典缓冲区及假名映射随应用资源打包，清单记录源文件和解压后 SHA-256。附带许可证。 | Xcode 27 产物中的资源路径、安装包大小待 CI 检查。 |
| 原生生成 | Swift actor 加载词典，执行候选词图、连接代价和 Viterbi；移植韩语发音规则、假名转写、语言和覆盖率判定。源引擎 8 组输出已导出为共享夹具。 | Swift 与源夹具对照测试待 Xcode 27；更大语料及特殊读音仍需核查。 |
| 真实页面 | 选定歌词及缓存命中后异步生成；可靠日韩结果优先，失败保留来源音译。画布用真实节点索引绘制韩语列和逐词遮罩；原文缓存不变。 | 当源时间节点跨越生成词边界且无法可靠拆分时仍回退行级，需设备上的复杂排版与动画对照。 |
| 用户入口 | 原音译开关继续控制显示；二级页提供状态、强制重新生成、生成缓存清理、ja/ko 纠错字典导入导出和重置。 | VoiceOver、Apple 字体、HDR/Plus Lighter 合成以及 60/120 Hz 真机验收。 |
| CI | `b594aca1` 的直接 IPA 已通过，Actions run `36119202213`，上传产物约 21.26 MB；两个任务均增加固定资源哈希检查。 | 模拟器测试后 IPA 仍在排队；包内每个字典资源及体积新增独立检查，待下一次构建。Apple 设备视觉验收仍缺失。 |

此移植不新增音乐播放服务，不把来源音译写回原始歌词。以上状态不代表设备视觉验收完成。

## 2026-09-25 逐词音译去重与阅读顺序

- 当前原生画布若已显示逐词音译，来源同时附带内容相同的整行音译时，不再重复绘制整行；内容不同的独立行级音译仍保留。仅规范化空白作去重判断，不改写来源或缓存。
- VoiceOver 按正文词、对应音译交替朗读，再读独立行级辅助文字；关闭音译时不朗读隐藏音译。模糊行和设置切换也更新阅读文本，逐帧位置变化不重复生成标签。
- 新增重复来源、独立行来源和开关回归。Xcode 27 模拟器与 IPA 工作流验证状态以对应提交的 CI 为准；Apple 字体及 VoiceOver 实机验收仍待设备。

## 2026-09-25 逐词音译接线修正

- 来源词音译保留为单一容器及原词归属，嵌入空格不再导致注音丢失；相邻拉丁音节仍保持同一断行/强调 chunk。
- 独立音译时间通过 romanStart/romanEnd 兼容保存，布局遮罩直接消费；相同时复用正文时间。解析版本升至 5，旧原始载荷可重解析。
- TTML 与辅助资源使用统一互唯一匹配，拒绝并列、跨多词整行注音及声部不明的行匹配，不覆盖已有明确音译。
- 原版浏览器 7 组多语言夹具实测通过：半字号、1em 行高、0.3em 末端留白、缺词占位。原生修正无 ruby 的 start 对齐、紧凑行高和重复降低 alpha；ruby 容器继续居中。
- 主动差异：原版分词会复制内部含空格词的整段 romanWord，本实现按用户要求只保留一份，不生成分词注音时间。
- 新增 Swift 来源/时间/缓存兼容/多声部/Unicode/开关回归；本地 SwiftFormat 和 12 项 Node 回归通过，新增 Swift 用例等待 Xcode 27。原版探针命令：AMLL_ROMANIZATION_PROBE=1 node Scripts/check-amll-controlled-browser.cjs（PowerShell 使用环境变量赋值）。
- 仍待验证：同 Apple 字体基线、HDR/SDR 合成真机显示、完整暂停/seek/回收动画图像对照及 VoiceOver 实机阅读；不能据此宣布逐字音译已完整视觉验收。


## 2026-09-20 资源计数接入

Debug 共享回放逐帧导出可见行、回收行、布局和实际 CALayer（含遮罩）数量。新增 150 行、900 秒虚拟遍历回归，检查回收上限 12 行及布局/图层随保留行数有界；等待 Swift CI。该测试以 5 秒步长遍历歌曲，仅验证保留策略，不是 15 分钟真机热状态、GPU 纹理或掉帧率通过证明。

## 2026-09-20 强调分组、缓存完整性和叠片工具

- 对照原版 `LyricLineEl.buildWord/initEmphasizeAnimation`，强调按布局 chunk 合并时间与字符序列，再映射回音节字符；浮动和遮罩仍保持音节自己的时间。修正 UTF-16 强调资格与原版末词 substring 判断；按 chunk 一次生成关键帧，避免逐字符重复创建整个数组。新增合并短音节和 emoji 回归，待 CI。
- 动态封面缓存持久化完成时字节数；命中时拒绝被截断、清空或系统删除的文件，兼容旧索引。同大小损坏内容仍由既有解码失败回调拒绝。新增重建后截断、删除及凭据保密回归，待 CI。
- 新增原尺寸 PNG 叠片与差图页面，校验输入哈希/字体/帧/视口，不自动对齐。11 项 Node 测试通过；Edge 实测 PNG 上传、元数据校验及差图通过。使用方法见 `Visual-Diff-Workflow.md`，不代表生产截图已通过。
- CI 按 direct/tested 隔离并发；direct 可替换旧构建，正在执行的 tested 不因新提交中断。`33bc0b51` direct 已成功，tested 正在 iPhone 模拟器测试；尚无最新通过结论。
- 未闭合：完整参考宿主、场景切歌/尺寸事件、跨端坐标规范化与实际差异报告、背景像素对照及设备性能/显示验收。不能凭工具已可用标记全部完成。

## 2026-09-20 词来源映射与注音归属

- 分词结果增加原始词索引及词内 UTF-16 范围；Core Text 保存同一原子表，正文、字符、ruby/罗马音片段的 wordIndex 可直接回溯来源。空格、CJK 拆分、组合字符与 emoji 保留完整范围，不推算 ruby 对应的正文字数。
- 一个带罗马音的来源短语拆成多个词时，不再把整段注音复制到每个子词；原始歌词数据保留，排版通过来源归属判断歧义并回退一次行级显示。独立行级罗马音规则不变。
- 注音动画改为直接读取原子表，移除每个注音每帧扫描正文片段的查找。普通浮动、SDR 亮度和真实分段遮罩保持原规则。
- 新增 UTF-16 完整性、原始词回溯、歧义注音单次回退回归；Windows SwiftFormat/diff 检查通过，Swift 测试待 Xcode CI。未据此宣称复杂文字像素或性能验收通过。

## 2026-09-20 共享事件场景接线

- 原生 Debug 和固定原版浏览器现在读取同一份 `amll-shared-replay.json`，不再分别硬编码不同操作。480 帧包含暂停、seek、浏览开始/移动/结束和恢复；原生 120 Hz 将每帧一分为二并同步映射事件，保留事件发生时刻。
- 浏览器直接驱动原版触摸事件；非零松手速度缺少真实触摸轨迹时明确拒绝，不能假装与原生惯性相同。两端浏览恢复规则的产品主动差异继续保留。
- 原版浏览器完整共享场景重建运行两次，全部帧一致；9 项 Node 回归通过。新增 Swift 共享资源与双刷新率事件时刻测试，待本次 CI。268 个构建输入哈希固定，原依赖未升级。
- 源码核对 `createWord/buildWord`：ruby 与 romanWord 是普通浮动容器的子层；逐字符强调只对正文 `subElements` 初始化。不能为辅助文字额外复制正文强调缩放；当前普通浮动接线与该父子关系一致，复杂分组映射仍待对照。
- 当前仅统一歌词资源引用及时间事件；字体/配置/资源哈希契约、切歌/尺寸事件、跨端规范化和叠片仍未完成，未宣称 P2 整体完成。

## 2026-09-20 最新验证

- `7f190adf` 的两路 Xcode 27 CI 均成功（run 35450992409）：直接 IPA 和模拟器测试后 IPA。包括本轮注音容器、视频状态与原生回放增量；不是设备视觉签收。
- P1 正文/ruby/逐词罗马音已按联合容器宽度参与断行，宽罗马音不再仅因宽度回退行级；修正 RTL 注音末端留白。完整来源映射与辅助强调仍待闭合。
- P2 原生 Debug 已支持真实画布冻结、前后帧重放与轨迹导出。浏览器新增受控 RAF、定时器、Date/performance 时间及 CSS/WAAPI 采样；暂停歌曲时间不暂停必要动画时间。
- Windows Edge 原版宿主实测：冻结画面不随真实时间变化；60/120 Hz 混合步进；180 帧暂停、seek、触摸浏览及恢复场景，两次重建宿主输出完全一致；独立逐字夹具冻结检查通过。动画记录按 DOM 目标及属性规范排序，保留同目标同属性的合成顺序，不按采样值排序。
- 7 项 Node 回归通过，79 个原文件及 266 个构建输入保持锁定；新增输入仅为开发时钟适配器。新增 `Scripts/check-amll-controlled-browser.cjs` 可重跑浏览器验证。
- 仍未完成：跨端共用完整场景与输入哈希校验、共同坐标归一化/叠片、完整参考页面、Mesh/Pixi 像素对照，以及 HDR/性能/辅助功能设备验收。本轮不标记完整移植完成。

## 2026-09-19 最新实施状态（覆盖下方历史结论）

| 范围 | 状态与证据 | 未闭合 |
|---|---|---|
| P0 基线 | `62aa80bc` 两路 Xcode 27 CI 成功（run 35436414736）；79 源文件/265 构建输入哈希已验证 | 最新增量 CI |
| P1 辅助排版 | 分段注音/逐词罗马音已有绘制；来源标记不雅词遮罩已接入且默认关闭 | 联合容器宽度、完整原子映射、辅助强调及原版对照 |
| P2 回放 | schema 2 轨迹比较及 4 项 Node 测试进入 CI | 共享事件场景、冻结 CSS/WAAPI、真实引擎步进、跨端叠片 |
| P3 HDR/背景 | HDR 已接入生产 Metal，2 倍目标；Mesh/Pixi 可显示 | 全屏/缩小/横竖屏设备合成、原版颜色与运动对照 |
| P4 视频 | 首帧/无进展 30 秒超时、独立播放状态、请求代次、后台倒影队列、坏缓存失效已接线；`97288606` direct IPA 成功 | 最新回归、视频/倒影耗时和设备可靠性验收 |
| 综合 | 保留 Apple Music 构图和现有 Spotify 回调；不新增音乐播放后端 | 目标设备视觉、辅助功能及 15 分钟性能签收 |

CI 保持两个独立任务：Xcode 27 / iOS 27 SDK 直接 IPA，以及模拟器测试后 IPA；不再运行 Xcode 26。模拟器测试不是物理真机测试。后台暂停保留同资源状态，覆盖下方旧“远程暂停释放”的实现说明。


## 2026-09-19 滑动绘制复用与轨迹检查

- 静止 SDR 行缓存行内绘制状态：仅父行位置/缩放变化时复用词图层，避免浏览期间反复写入遮罩、颜色和字符变换。禁用词时钟的反向动画结束后归一化缓存键；播放中的动画与 HDR 行仍逐帧绘制，seek、配置、亮度变化使缓存失效。新增暂停 120 帧复用及 seek 重绘回归，等待 Xcode 27。
- 新增 `node Scripts/compare-amll-traces.cjs reference.json actual.json`：比较 schema 2 原生帧导出，拒绝环境、断行、时间及显隐不一致，检查位置 P95 ≤1pt、透明度最大差 ≤0.02、缩放最大差 ≤0.002。4 项 Node 回归通过。该工具用于同格式回归，不替代原版浏览器适配、冻结 WAAPI、像素叠片或真机签收。
- 原版 79 个文件和 265 个构建输入哈希重新验证通过。HDR 目标按最新要求为 2 倍 SDR 白；实际设备余量仍限制输出。
- HDR 生产接线测试增加窗口生命周期保留及帧/字形纹理前置断言，待 CI 确认失败原因。未将滑动性能或全部移植标记为完成。


## 2026-09-19 在线封面与缓存解耦

用户反馈静默下载一直加载。修正此前“必须缓存完整才播放”的实现约束：当前页面在允许网络下先使用 HTTPS 无声视频，后台静默缓存最多等待 120 秒，失败不阻塞在线显示；成功后切换本地。网络限制直接传至 AVURLAsset，远程暂停释放。此为处理系统 discretionary 无限排队的主动调整，不改变 200MB 缓存和默认 Wi-Fi 策略。流播放、循环、音频共存仍待真机验证。

## 2026-09-19 双任务构建策略（最新）

每次 Actions 启动两个独立矩阵任务：tested 完成 iOS 27 模拟器单元/UI 测试及 iPad 构建后产出 IPA；direct 不等待测试，直接产出 IPA。`fail-fast: false` 保证测试失败不取消 direct，产物名称区分 tested/direct。direct 成功不代表测试、真机 HDR 或视觉验收通过。可用 runner 数量决定实际能否同时运行。

## 2026-09-19 CI 策略更新（覆盖此前直接 IPA 要求）

按用户最新要求恢复此前移除的 Xcode 27 / iOS 27 测试阶段：iPhone 模拟器单元/UI 测试、iPad 模拟器构建、测试结果及截图附件。后续 archive/IPA 必须等待测试成功。仍不使用 Xcode 26。此前流程实际为模拟器测试，仓库没有连接实体设备及签名配置，因此不将此次恢复称为真机测试通过。运行时选择脚本的两项 Node 回归已在本地通过，Apple 测试结果以新 Actions 运行结果为准。

## 2026-09-19 全屏与交互缩小合成差异

用户截图 IMG_0114/0115 及操作描述表明，文字效果随系统 zoom 的全屏/交互缩小状态变化。候选修正：整页背景和文字固定合成分组，iOS 26+ 显式 allowedDynamicRange(.high)。保留真实 EDR 余量、HDR 配置及 SDR 辅助文字规则。此修正尚未完成设备 A/B；PNG 不能验证 HDR 亮度，不能把候选修正记为已签收。应对同一句暂停歌词，比较全屏、拖住缩小、取消后全屏三态。

## 2026-09-19 动态封面生命周期与缓存替换

- HDR 调度提交 `742f0469` 的 Actions `35416182638` 已成功直接构建 IPA。
- 动态视频逐个观察 AVPlayerLooper 当前 item 的 tracks/status：音轨加载后显式禁用，继续保持 muted/零音量；循环或切歌重新绑定，迟到回调核对当前 item。失败隐藏视频并清除反射，露出已有静态封面，不新增音频会话。
- 缓存替换先移动到独立新文件并验证容量，再删除旧资源；下载文件缺失或超限不会先删除同 URL 的有效视频。缓存目录被系统清理后可重新创建。
- 新增缺失文件/超限替换/成功替换/重建索引回归源码。SwiftFormat 和 diff 检查通过；Windows 未执行 Swift 测试。循环视频与外部音乐共存仍待设备验证，不将 P4 或全部移植勾选完成。

## 2026-09-19 HDR 绘制调度

- 共享歌词夹具提交 `68557ec6` 的 Actions `35415564699` 已成功完成直接 IPA 构建。
- 生产 HDR 行与 Debug 验证入口先取得 GPU 在途名额，再申请 drawable；忙碌帧返回原有 SDR 路径，失败申请释放名额。减少不必要的 drawable 等待，不代表所有 Core Animation 等待均已消除。
- 新增回归源码验证三帧容量、失败释放及空帧不申请。Windows SwiftFormat、diff 检查通过；Swift/GPU 测试未执行。按用户要求保持直接 IPA 流程。
- 复杂辅助排版、完整冻结回放、视觉及性能签收仍未完成。并行审查因额度限制未运行，已自行复核本补丁。

## 2026-09-18 Mesh 多网格接线

- 原版六组预设及 20% 随机控制点路径接入真实 Metal 背景；每个背景上下文独立种子，Debug 可传固定种子。
- 每张封面独立网格、纹理和透明度；按原 delta/500 推进新状态，easeInOutSine 合成，达到 1.1 后释放旧状态。无封面按原规则淡出。
- 每个网格先绘制离屏纹理再混合到屏幕，避免网格折叠区域重复混色；中间纹理随视口重建，GPU 提交最多三个在途。后台/低动效暂停时保持动画时钟，恢复不会把后台耗时作为单帧。
- 新封面准备前继续保留旧背景；初始空背景保持中性灰。像素钳制使用与 JS Uint8ClampedArray 相同的 ties-to-even 舍入。
- 代码审查完成；Xcode 归档待运行。本节仅闭合 Mesh 切换接线，Pixi、整页视觉/性能签收尚未完成。


## 2026-09-18 背景基准与回归修正

- 提交 265 个 core 浏览器构建输入的 SHA-256 清单和验证脚本，本地验证通过。仍不代表完整 react-full 页面资源锁定。
- 提取原版 6 组 Mesh 预设；生产默认网格读取精确首组预设，保留资源缺失回退。新增 Swift 随机控制点生成器及 4 组原 TypeScript 固定种子夹具；生成器暂未用于生产随机切换，Swift 对照测试未执行。
- 切歌下载期间保留上一张背景，避免灰色闪屏。多网格交叉混合、Pixi、竖形封面仍待实现。
- 修复缓存根路径符号链接归一化及淘汰失败时超限准入；无可用 EDR 余量/非活动行回退原 SDR 图层，避免额外 Metal 提交。
- 9070f676 历史测试：196 个单元测试中 2 项失败、5 个 UI 测试全部通过。跑马灯失败为 CAKeyframeAnimation 数值桥接检查，已显式使用 NSNumber；HDR 创建测试增加确定性 EDR 能力输入。修正尚未重跑，不能标记测试通过。
- 074f9ba9 的直接构建 run 35351414356 已成功，包含 Release archive、应用包检查和未签名 IPA 上传。


## 2026-09-18 直接 IPA 构建策略

按用户最新要求，CI 取消 iOS 27 设备/模拟器测试及模拟器构建，使用 Xcode 27 / iOS 27 SDK 直接 Release archive、检查应用包并上传未签名 IPA。保留测试源码，未执行的测试不算通过；HDR/视觉签收与性能仍未完成，不作为本次 IPA 构建前置条件。


## 当前 CI 门槛（2026-09-18 用户更新）

后续统一使用 Xcode 27 / iOS 27 SDK 执行构建、单元/UI 测试、iPad 构建、archive 和 IPA 检查，不再运行 Xcode 26 测试。下文的 Xcode 26 结果仅为历史证据，不是新增任务要求。工作流保留 iPhone 16 Pro 与 13 英寸 iPad Pro 参考型号，并显式创建 iOS 27 模拟器。

已复核 `ed82076e`：182 个单元测试全部通过（含 HDR GPU 读回与辅助文字），5 个 UI 测试中 1 个因两个“显示歌词”按钮同名导致选择歧义而失败。`c320883f` 为菜单动作增加独立标识；新的 Xcode 27 全套测试仍需运行确认。

## 2026-09-18 方形动态封面接线（待 Xcode 27 验证）

- 真实页面复用 Apple 目录查询；非 Apple 歌词来源只单独查询封面候选，不修改歌词来源或人工匹配。方形模式不裁切竖形视频冒充方形资源。
- 新配置默认关闭，开启后默认仅 Wi-Fi，蜂窝单独授权；网络策略变化、切歌和退出页面取消旧任务，迟到结果由请求代次拦截。离线、减少动态效果、无匹配或下载失败保留静态封面。
- 通过公开 AVAssetDownloadURLSession 下载 HLS，渐进视频通过 URLSession 下载；AVQueuePlayer 只接收完成缓存的本地 URL，始终静音，不配置音频会话、不发布音乐播放状态。首次视频帧就绪前保留静态图。
- 独立 200 MB 缓存按最近访问淘汰；HLS 包保留系统位置，索引记录沙盒相对路径。新增清理入口、超限拒收、缓存恢复、静态回退与切歌迟到回归测试。
- 竖形/沉浸反射仍未完成；HLS 真机下载、蜂窝切换、视频与 Spotify 同时运行、缓存性能仍待验证。不能以此把 P4 或全部移植标记完成。

## 2026-09-18 生产 HDR 首次接线（未签收）

- AMLLNativeCanvas 逐帧读取实际窗口的 EDR 余量；实际歌词时间决定活动句，暂停保留、seek/offset 更新重算。已有 Core Text 字形图集、词遮罩和字符变换进入浮点 Metal 替换层，成功提交后隐藏对应 SDR 正文字形，失败回退 SDR。
- 原生歌词画布接入 Plus Lighter；减少透明度使用 normal。辅助文字不传入 HDR 增益，普通长音 glow 单独以 SDR 计算；新增默认关闭的“当前句 HDR 高光”和屏幕能力提示。
- 生产接线新增图层创建/移除测试，原 GPU 像素读回继续验证替换亮度。GPU 队列最多保留三个在途提交，布局和纹理随既有行缓存释放。
- **仍需闭合**：模糊过渡目前仍使用既有 SDR 整行位图，高光随清晰层权重退出；Metal glow 采样与原 CALayer shadow 尚无同帧对照；SwiftUI 混色/淡出是否保持 EDR 以及实际输出亮度、60/120 Hz 和长时性能未真机验证。因此不标记 P1 完成。

## 历史阶段快照（当前状态以上方 2026-09-18 记录为准）

2026-09-12，起始基线 `707e8f1a`。以用户批准的《AMLL 剩余视觉能力移植与验收计划》为本轮规范。保留 Apple Music 页面构图、28% 默认定位、浏览无模糊与短惯性、下一句恢复跟随；不增加音乐播放后端。

| 阶段 | 当前证据 | 尚未完成 |
|---|---|---|
| P0 | 当前直接 IPA 策略已生效；d6397734 的 Xcode 27 归档/包检查通过。已固定 265 个 core 浏览器输入及 Pixi 滤镜输入，导出原版 Mesh/Pixi 数值夹具 | 完整 react-full 页面共享冻结回放、全量资源与前景叠片；历史失败测试修正未重跑，不计通过 |
| P1 信息文字 | 页面标题、艺人、专辑与歌词画布接入 SDR Plus Lighter；减少透明度回退 normal | 实际合成、署名位置及辅助文字视觉签收 |
| P1 HDR | **已接入生产可选路径**：浮点 Metal 替换主字形、活动句已唱范围、变换/遮罩、实时显示余量、设置及能力提示；SDR fallback 保留原字形 | 长音发光与模糊过渡的完整视觉对照、HDR 真机亮度及合成验证；不是仅模型，也尚未签收 |
| P2 | 分段 ruby 时间/UTF-16/运行段映射与遮罩；逐词罗马音及有诊断的行级回退；非动态整行测量校准已接线 | 注音宽度对正文排列、复杂/跨行 ruby、完整 Intl/NL 分词与断行对照、辅助文字强调；最新测试未执行 |
| P3 | 六组 Mesh 预设/随机网格/封面过渡；独立 Pixi 四层封面、多通道滤镜与保留上下文；纯色/原生双色渐变已接线 | 各模式 GPU 边缘、色彩、光流/完整源版逐帧对照及设备性能；不将构建通过视为背景签收 |
| P4 | 方形/竖形资源查询与静态回退、无声视频、默认关闭/Wi-Fi 策略、200MB LRU；同视频可选反射、跑马灯和真实署名入口已接线 | 视频循环/失败恢复、反射主线程开销、原插件完整48pt过渡层、署名位置及目标设备视觉验证 |
| 综合签收 | 无本轮真机报告；按用户要求不以真机测试阻止直接构建 IPA | 15 分钟稳定性、60/120Hz、目标设备 HDR/视觉及辅助功能签收仍缺失；全部移植保持未完成 |

HDR 实施语义固定为：每个真实活动句的已填充区域持续高光，直到该句结束；不是仅正在发声的词。逐行歌词激活整行，不制造逐词时间。歌词之外的信息文字保持 SDR；HDR 目标为线性白 1.5 倍并受实时余量限制。关闭 HDR 不关闭 Plus Lighter。普通长音 shadow 不算 HDR。

公开 API 依据：[SwiftUI BlendMode](https://developer.apple.com/documentation/swiftui/blendmode)、[Metal EDR 与浮点输出](https://developer.apple.com/documentation/metal/performing-your-own-tone-mapping)。使用浮点格式本身不代表已完成 HDR，必须验证实际显示输出。

所有阶段保持未勾选；不得用模型测试或 SDR 截图替代 Metal 接线与真机签收。

## 2026-09-13 真实页面跑马灯

- `react-full/src/components/TextMarquee/index.tsx` → `AMLLMarqueeMotion` / `AMLLMetadataText`：保留 95% 宽度阈值、32pt/s、一次往返，静止右侧/运行双侧淡出。没有沿用旧近似无限循环。
- 紧凑与展开页面的标题、艺人、专辑统一接线并保留原字号及 SDR Plus Lighter；字号遵循 Dynamic Type/Bold Text。跑马灯设置在 AMLL 页面也可操作。
- 鼠标进入/离开对应原版事件；点击触发/停止为触屏主动适配。减少动态效果、关闭设置、切歌/文字变化、离屏与后台会清除动画；完整文本由 VoiceOver 读取。
- 数据中的真实歌词作者/歌曲作者可在菜单查看，不生成虚假署名。菜单并非原版署名位置，不计为完整署名视觉移植。
- 新增轨迹数值与实际 UILabel/CAAnimation 接线测试，尚待 CI；生产 HDR、背景及动态封面仍未完成。

### 动态封面目录发现基础

已按本地插件 0.29.21 实现 `歌曲 include=albums → 专辑 extend=editorialVideo&platform=web`，解析 `motionDetailSquare` / `motionDetailTall` 的字符串、url、href、video 包装。资源结果保留专辑和 storefront；请求取消在跨请求边界检查。缺少专辑关联返回无资源，损坏专辑响应报告错误。

这只是可测试的目录发现方法，尚无页面调用、匹配调度、无声视频、Wi-Fi 策略或媒体缓存。不得把资源 URL 查询当成 P4 完成。新增 URL 形态和请求合约测试；来源 SHA-256 见 `ReferenceCaptures/remaining-visual-source-hashes.json`。

## 2026-09-13 HDR 浮点绘制验证入口

- 新增 `LyricsHDR.metal` 与 `LyricsHDRRenderer`：RGBA16Float、extendedLinearSRGB、透明 CAMetalLayer。着色器按原字形 alpha 和羽化覆盖计算 SDR/HDR 替换值，不重复叠加普通白字。
- Debug 歌词预览增加可折叠 HDR 字形验证区，使用真实 Core Text 栅格及 AMLLWordMask，拖动 0–5 秒验证已填充/未填充/失活状态。每次绘制查询窗口屏幕 current/potential EDR headroom，目标不超过 1.5；HDR 关闭仍保留 SDR Plus Lighter，减少透明度采用普通混色。
- 新增 GPU 浮点像素读回测试：已填充 1.5、未填充 0.3、无字形区域透明；另测图层色彩空间/格式/尺寸。尚未在 Windows 执行这些 Apple GPU 测试。
- 此入口不是生产歌词引擎。生产动画、blur、shadow、转场和 EDR 合成仍需统一接线及验证；不开放生产 HDR 开关，不标记 P1 完成。

## 2026-09-13 辅助文字接线

- `AMLLCoreTextLayout.RubyFragment` 保留词/分段索引、UTF-16 范围、视觉运行方向及原时间。渲染器复用缓存注音栅格，按分段时间更新遮罩，不生成注音时间。
- 每个换行正文均预留注音高度。跨行词目前仍将 ruby 放在首个视觉行上；不计为完整跨行 ruby 移植。
- 有明确对应且宽度可容纳的逐词罗马音放在词下方。过宽或分词复制造成对应歧义时保持行级文本，Debug 轨迹 schema 2 导出逐行 diagnostics。服务端独立行级罗马音继续保留。
- 新增分段时间/范围、换行留白、罗马音开关及回退测试。Windows 仅执行格式和 diff 检查，Swift 测试与视觉效果待 CI/真机。
- HDR Metal、辅助文字 Plus Lighter、完整 Mesh/Pixi、动态封面与综合验收仍未完成；本轮未开放这些未完成能力的开关。

## 2026-09-13 基线复核

`0f173e32` 的 CI 34699214261：Xcode 27 build/archive/IPA 通过；模拟器创建成功，Xcode 26 执行 176 个单元测试（3 失败）及 5 个 UI 测试（2 失败）。新 HDR 模型测试通过。浏览恢复真实边界的产品错误已修复；字号与文本保留测试已按来源及主动差异修正。UI 测试更新到当前画布，并为初始入口缺失收集诊断；等待新运行确认，不能宣称已修复全部 UI 问题。

原 CI visual-review artifact `10299841954` 的界面树确认音乐信息按钮存在，但 identifier 为 `miniPlayerBar`，其容器标识覆盖了 `openNowPlaying`。已在系统底栏容器增加 `.accessibilityElement(children: .contain)`，与旧迷你栏保持一致；不是通过绕开入口让测试通过。待新 CI 确认。

## 2026-09-19 竖形动态封面接线

- 新增可选沉浸封面布局：按资源类型选择竖形视频，使用原 react-full 的封面中心/范围公式、70% 遮罩边界和 200/30 弹簧。保留 Apple Music 前景构图，属于明确布局差异。
- 读取实际封面槽位置，覆盖手机及 iPad；横屏取消不可见竖形视频请求并恢复静态布局。切换模式/歌曲沿用 revision 校验，旧方形或竖形资源不会串用。
- 旧配置缺少 presentation 时仍为方形；动态封面和蜂窝策略不自动开启。新增旧配置、资源选择、重置及几何回归测试，按直接 IPA 策略未执行 Apple 测试。
- Mesh 提交 04c0941a 的直接 IPA 构建 run 35352753867 成功。此次封面修改仍待新归档；Pixi、反射、完整视觉/HDR/性能签收仍未完成，不标记全部移植完成。

## 2026-09-19 Pixi 独立原生管线

- 新增独立四层封面 Metal 绘制、原版旋转/位移、逐容器淡入淡出、五档及大视口追加模糊、饱和度/亮度/对比度与双 bulge 滤镜。使用实际 .75 渲染比例和 30Hz 背景时钟；歌词时钟独立。
- 背景模式可选 Mesh/Pixi，旧配置仍选 Mesh。使用过的上下文保留，隐藏模式不发起新封面下载；普通暂停保留过渡状态，减少动态效果单独静态化。未呈现旧资源在新资源安装时释放。
- 直接执行固定 Pixi onTick 导出 60/120Hz/不规则轨迹夹具，锁定滤镜与 ticker 源码 SHA-256，补充 MIT 归属。原版 ticker 的 100ms 上限只用于 Pixi 帧入口，不改变歌词时间。
- 本地原参考生成、265 项资源哈希检查和 diff 检查通过。Swift 新测试未执行；GPU 滤镜边缘、色彩、内存与真机视觉仍待对照，不以管线接线宣称 1:1 完成。
- 竖形封面提交 1cd82fe2 的 run 35371698542 已成功 archive/IPA。本次 Pixi 待新构建；反射及综合签收仍未完成。

## 2026-09-19 同源视频倒影与静态背景

- 可选倒影使用同一 AVQueuePlayer 的 AVPlayerItemVideoOutput；直接传给原生图层，不以逐帧 SwiftUI 状态驱动。按原插件截取底部 24%、镜像、9pt 模糊、1.06 饱和度、0.24 透明度及原渐隐节点，保留透明边界和外层裁剪。
- 原版反射槽采用 ceil(封面底边)+49pt、高度 clamp(视口高度*0.3,220,340)；本应用绑定 Apple Music 实际封面槽，属于已保留的构图差异。切换循环 item 重新绑定 output，停用/关闭取消 display link，来源 token 拦截旧帧。
- 新增纯色（原默认 #111111）及原生双色渐变选项。原版 CSS 可输入任意字符串但没有内置渐变预设；本应用仅提供固定自上而下双色渐变的原生编辑，不新增 CSS 执行器。
- d6397734 的直接 IPA 构建 run 35414204666 成功。此前 64efeb3f 因 SDK 异步纹理加载缺少 await 失败，已修复并重检迟到资源。当前倒影/静态背景待新构建。
- Windows 无法执行 Apple 图形测试。逐帧 Core Image 倒影性能、循环取帧及 HDR/辅助文字/完整背景视觉签收仍待验证；全部移植保持未完成。

### Pixi 包装层默认值校正

再次核对 PrebuiltLyricPlayer/configAtoms，完整页面显式覆盖 core 构造器参数：默认背景帧率为 60、渲染倍率为 1。原生页面已按包装层校正；前节 30Hz/.75 描述仅适用于独立 core 构造器，不再作为本应用默认。此修正不涉及歌词刷新率或自动性能降级。

## 2026-09-19 非动态排版校准

逐行歌词先用完整 Core Text 塑形宽度校准孤立子项宽度，再运行原断行代价函数，对应 line-balancer 的 needsCalibration 路径；逐字路径不套用此比例。新增能改变断行决策的回归用例，并保留字串/空格及无词时间语义。Intl.Segmenter 与 NLTokenizer 的各语言边界仍需对照，不能据此宣称全部复杂排版完成。

同视频反射与静态背景 a35aad5c 的 run 35414417113 已完成 Xcode 27 archive、应用包检查与 IPA。最新测试源码未执行。当前状态表已按生产接线更新，历史记录保留；下一构建包含包装层默认值与本次校准。

## 2026-09-19 共享参考输入

- 浏览器与原生 Debug 预览改为共同读取 amll-shared-lyrics.json，保留原始毫秒时间、背景声部、对唱及翻译；默认字号 32、定位 28%。原生按钮可载入共享歌词或恢复复杂文字夹具，读取失败明确显示错误。
- 浏览器重新构建通过，79 个原版文件验证通过；265 个输入哈希更新仅反映本地参考入口/共享数据的明确变更，原依赖未升级。再次验证清单通过。
- 这只闭合共享歌词输入，不代表冻结 CSS/WAAPI 时钟、完整页面/封面脚本、同字体断行或视觉签收完成。
- b2c6a01d 的直接 IPA run 35414674518 成功，包含反射、背景模式与非动态测量校准。共享预览修改待下一归档；测试源码仍未执行。

## 2026-09-19 HDR 字形上传故障定位
- b17490a9：直接 IPA 成功；212 项单元测试中 HDR 接线测试出现 2 个断言失败，5 项 UI 测试通过。滑动复用/seek 回归通过。新增前置检查确认失败发生在真实 Core Text 图像上传，而非行选择或窗口。
- 将字形 atlas 显式归一化为 RGBA8 透明位图后上传，避免依赖 MTKTextureLoader 对 UIKit 图片格式的支持。保留独立浮点输出和 2 倍亮度。新增四角 alpha/方向 GPU 读回回归；待 CI，不宣称真机 HDR 已签收。

## 2026-09-19 动态封面解码失败回传
- 方形/沉浸视频的实际 AVPlayerItem 失败回传加载器，清除视频资源并显示失败状态；校验歌曲与资源地址，旧回调不能影响新歌。请求代次失效，迟到缓存不能重新启用解码失败视频。静态封面继续托底。
- 新增解码失败/迟到缓存/旧歌回调/新请求恢复回归，待 Xcode 27。并非视频缓冲超时或真机循环验收完成。

## 2026-09-19 新计划 P0 与 P1 内容显示
- 5810ea55 / run 35434787652：214 个单元测试、5 个 UI 测试通过；direct/tested 两条 IPA 构建成功。HDR 实际字形上传、透明度方向及滑动复用回归已通过自动验证，仍非真机 HDR 签收。
- 轨迹比较的 4 项 Node 回归接入两条 CI；265 个浏览器输入哈希验证通过。
- 不雅词遮罩接入原生显示副本与高级设置，默认关闭、可保留首尾或全部遮罩，默认字符 *。不修改来源缓存或真实时间；VoiceOver 使用显示副本。Swift 按 grapheme 遮罩，明确区别于原 JS UTF-16 正则，避免拆坏 emoji/组合字符。新增 3 项回归，待 CI。
- P1 注音容器排版/动画、P2 受控回放、P3 视觉及 P4 性能可靠性尚未闭合。

## 2026-09-19 P4 视频等待与倒影队列

- 已接线待验证：动态封面增加独立于下载的 30 秒首帧/无进展超时；暂停、后台不累计，同一资源暂停保留预算。失败清除倒影并通知页面回退。
- 视频呈现状态与资源缓存状态分离；首帧显示后才报告播放。页面回调携带请求代次，旧请求同 URL 回调也不能污染新请求。
- 倒影 Core Image 栅格化移入专用串行队列，最多一个在途帧与一个最新待处理帧；换资源、尺寸变化、关闭会作废旧输出。
- 新增三项 watchdog 回归。Windows 完成 SwiftFormat、diff 检查和 Node 轨迹测试；Swift/Metal、实际超时及倒影主线程耗时待 Xcode 27 CI 和设备验证。P4 缓存损坏淘汰及完整设备验收仍未完成。

### P4 损坏媒体淘汰

解码/呈现失败后按实际本地文件标记不可用并尝试清理；不能删除时保留所有权但不再命中缓存。迟到的旧文件失败不会删除同 URL 的新替换文件。新增持久化/重新下载与同歌曲同 URL 请求代次回归。状态为已接线待 CI。

## 2026-09-19 P1 注音容器宽度

按固定 core `wordWithRuby`/`wordBody` 的 inline-flex 居中布局，带注音的逐词行使用独立词容器，宽度取正文、ruby、开启的罗马音最大值，并计入原版断行代价。移除仅因罗马音更宽而回退行级的判断；歧义对应仍回退并诊断。Core Text 提供词内塑形及整行双向视觉顺序，片段保留逻辑 UTF-16 范围。辅助片段接入所属词普通浮动，保持 SDR，未叠加主词 HDR。

新增长注音参与断行、罗马音扩展居中回归；旧超宽必回退断言按批准计划替换。Windows 仅格式检查；完整原版浏览器/Apple 字体对照、辅助强调及复杂双向边界仍待验证，不标记 P1 完成。

## 2026-09-19 P2 原生受控回放入口

新增版本化 AMLLReplayScenario/事件游标，显式分离歌曲位置与帧间隔；验证事件顺序、数值、seek 参数。Debug 歌词预览新增独立回放页，实际驱动生产画布布局/图层；60/120Hz 冻结步进、从起点重放后退、暂停/seek/浏览/恢复脚本及 schema 3 实际帧导出。事件先零时长提交，再推进动画，避免暂停边界沿用上一帧播放状态。不会发送 Spotify 或生产页面回调。

此入口尚不等于完整 P2：浏览器 CSS/WAAPI 虚拟时钟、资源/字体哈希合同、切歌/尺寸事件、跨端坐标归一化及像素叠片仍未闭合。新增游标回归等待 Xcode 27；Node schema 2 比较测试 4 项通过，不混用 schema 3 导出。
